wiremock.reset-mappings-after-each-test=true
wiremock.port=9000
notify.serviceUrl=http://localhost:${wiremock.port}/notification_service
oauth.serviceUrl=http://localhost:${wiremock.port}/identity
registry-service.serviceUrl=http://localhost:${wiremock.port}/csrs
catalogue.serviceUrl=http://localhost:${wiremock.port}/learning_catalogue
logging.level.root=DEBUG
